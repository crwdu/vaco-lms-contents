import { Callout } from "nextra-theme-docs";

# React Introduction

# Meet React.

_A library for creating User Interfaces._

_Renders your UI and responds to events._

_The **V** in **MVC**._

[https://www.youtube.com/watch?v=x7cQ3mrcKaY](https://www.youtube.com/watch?v=x7cQ3mrcKaY)

# **React Rendering**

아래와 같이 간단하게 우리는 React를 시작할 수 있습니다.

```jsx
const element = <div>자, 여러분..</div>;
const root = document.getElementById("root");

ReactDOM.render(element, root);
```

**🖥 _Example Link_**

[Introduction to React - 1](https://codesandbox.io/s/introduction-to-react-1-jxbjt)

**📖 _Rendering Elements_**

[엘리먼트 렌더링 - React](https://ko.reactjs.org/docs/rendering-elements.html)

**📖 _JSX_**

[JSX 소개 - React](https://ko.reactjs.org/docs/introducing-jsx.html)

## JSX

_JSX is an **optional preprocessor** to let you use HTML-like syntax._

_The accessibility of templates and the power of JavaScript._

```jsx
// Not using JSX
const element = React.createElement("div", null, "자, 여러분..");
const root = document.getElementById("root");

ReactDOM.render(element, root);
```

- `React.createElement` 함수의 첫 번째 인자는 DOM Element Type을 나타냅니다.
- 두 번째 인자는 Attribute를 나타냅니다. (React에서는 Element의 Attribute를 **Props**라고 부릅니다.)
- 나머지 인자는 Child Element입니다.

<aside>
🚀 `React.createElement`에 대해서는 간략히만 살펴보아도 충분합니다. 여러분은 주로 JSX를 이용해 React를 사용할 예정입니다.

하지만 원리를 알고 사용하는 것이 좋겠죠? 주어진 시간 내에 최대한 깊이 있게 학습하는 습관을 들이세요.

</aside>

**🖥 _Example Link_**

[React without JSX](https://codesandbox.io/s/react-without-jsx-evdj8)

**📖 _JSX 없이 사용하는 React_**

[JSX 없이 사용하는 React - React](https://ko.reactjs.org/docs/react-without-jsx.html)

```jsx
const element = React.createElement("div", { className: "something" }, "hello");
const root = document.getElementById("root");

ReactDOM.render(element, root);
```

Child Element란, 단순 텍스트가 될 수도 있지만 또 다른 React Element가 될 수도 있습니다. 그리고 또한 여러 개의 Child를 가질 수도 있습니다.

```jsx
const text = "I am a paragraph";
const element = React.createElement(
  "div",
  { className: "something" },
  "hello",
  React.createElement("p", { style: { fontSize: "30px" } }, text)
);

const root = document.getElementById("root");

ReactDOM.render(element, root);
```

모든 것은 Javascript입니다. 그렇기에 우리는 변수(`text`)를 사용할 수도 있습니다.

**위의 코드를 다시 JSX로 바꿔서 작성해보겠습니다. 몇가지 이해가지 않는 부분이 있겠지만, 위의 코드와 아래 코드를 비교했을때 어떤 차이가 느껴지시나요? 어느 쪽이 더 읽기 편하신가요?**

```jsx
const getText = () => "I am a paragraph";
const element = (
  <div className="something">
    hello<p style={{ fontSize: "30px" }}>{getText()}</p>
  </div>
);

const root = document.getElementById("root");

ReactDOM.render(element, root);
```

JSX에서 우리는 `{}` (중괄호) 를 이용하여 `{}` (중괄호) 내부에 Javascript Expression을 삽입할 수 있습니다. 위 예제에서 보이는 `getText()` 와 같이 말입니다.

<aside>
🚀 Javascript Expression이란, 결과값이 도출될 수 있는 코드 실행문을 의미합니다.

</aside>

**📖 _Javascript Expression_**

[표현식과 연산자 - JavaScript | MDN](https://developer.mozilla.org/ko/docs/Web/JavaScript/Guide/Expressions_and_Operators#%ED%91%9C%ED%98%84%EC%8B%9D)

# Components

_Use **components** to separate your concerns._

_Components are **composable**._

_Components are **unit testable**._

_Only put **display logic** in your components._

_No explicit DOM operations. **Declarative**._

지금까지 본 예제들에서 컴포넌트가 이미 사용되었습니다.

```jsx
const getText = () => "I am a paragraph";
const SomeComponent = () => <p>I am something else.</p>;

const element = (
  <div className="something">
    <SomeComponent />
    <p style={{ fontSize: "30px" }}>{getText()}</p>
  </div>
);

const root = document.getElementById("root");

ReactDOM.render(element, root);
```

위의 `element` 또한 컴포넌트화 해본다면 아래와 같이 함수로 변경할 수 있습니다.

```jsx
const getText = () => "I am a paragraph";
const SomeComponent = () => <p>I am something else.</p>;

const Article = () => (
  <div className="something">
    <SomeComponent />
    <p style={{ fontSize: "30px" }}>{getText()}</p>
  </div>
);

const root = document.getElementById("root");

ReactDOM.render(<Article />, root);
```

컴포넌트의 가장 큰 장점은 **재사용성**입니다. 앞으로 만들어 나갈 React Application의 중요한 초석이 되는 부분입니다. 현재 우리가 만든 `Article`이라는 컴포넌트는 항상 같은 UI를 보여주도록 되어있습니다만, React의 다양한 기능을 이용하여 우리는 상황에 따라 다른 내용을 보여주도록 사용할 수 있습니다.
